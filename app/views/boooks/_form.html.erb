<%= form_for(@boook) do |f| %>
  <% if @boook.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@boook.errors.count, "error") %> prohibited this boook from being saved:</h2>

      <ul>
      <% @boook.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :isbn %><br>
    <%= f.text_field :isbn, :maxlength => 13 %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :price %><br>
    <%= f.number_field :price %>
  </div>
  <div class="field">
    <%= f.label :publish_id %><br>
    <%#= collection_select(:boook, :publish_id, Publish.all, :id, :publishname, :include_blank => true) %>
    <%= f.text_field :publish_id %>
  </div>
  <div class="field">
    <%= f.label :published %><br>
    <%= f.date_select :published %>
  </div>
  <div class="field">
    <%= f.label :cd %><br>
    <%= f.check_box :cd %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
